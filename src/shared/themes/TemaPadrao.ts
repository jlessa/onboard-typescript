import { createTheme } from "@mui/material";

export const TemaPadrao = createTheme({
    palette: {
        background: {
            paper: '#fff',
            default: '#E7E8F0'
        }
    }
});